node_modules/
.artifacts/
test-results/
.vscode/
.codex/
.claude/scheduled_tasks.lock
.env
.env.*
!.env.example
md/CODEX_CHAT_PROMPTS.md
md/LOCAL*.md
md/LM_STUDIO_DEV_TESTING_GUIDE.md
md/AI_WORKSPACE_LM_STUDIO_OBSIDIAN_SETUP.md
md/REAL_LLM_AGENT_WORKSPACE_GUIDE.md
md/IMPLEMENTATION_HANDOFF.md
md/PERSONAL_REPORT_HE_2026-04-17.md
obsidian-vault/Reports/*
!obsidian-vault/Reports/README.md
obsidian-vault/.obsidian/
.obsidian/workspace.json
.obsidian/graph.json
# Local Obsidian app/UI state (drifts as the vault is used; not shared config)
.obsidian/app.json
.obsidian/appearance.json
.obsidian/core-plugins.json
asaf-1/

# Vite build output (React SPA) served by server.js at /app
public/app/

# Playwright blob shards + merged report (created by merge-reports and the
# remote test runner report job; the configured HTML output lives in .artifacts/)
blob-report/
playwright-report/

# Standalone test runner account store (created at runtime; never commit accounts).
test-runner/data/
test-runner/.env

# Local-only design and runbook notes, at the user's request 2026-08-28. NOTE:
# docs/remote-test-runner-runbook.md is the project's stated final deliverable
# ("so any AI can exute the same in the future") - ignored here means it exists
# on one machine only and a fresh clone will not have it.
docs/oidc-design.md
docs/remote-test-runner-runbook.md
